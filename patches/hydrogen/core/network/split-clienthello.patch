--- a/chrome/browser/hydrogen_flag_choices.h
+++ b/chrome/browser/hydrogen_flag_choices.h
@@ -36,6 +36,8 @@ namespace hydrogen {
       "hydrogen-noise-hw-concurrency";
 
   constexpr const char kDisableEchCommandLine[] = "disable-ech";
+  constexpr const char kHydrogenSplitClientHelloCommandLine[] =
+      "hydrogen-split-clienthello";
 
 }  // namespace hydrogen
 
--- a/chrome/browser/hydrogen_flag_entries.h
+++ b/chrome/browser/hydrogen_flag_entries.h
@@ -39,4 +39,8 @@
      "Use only QUIC v2 for HTTP/3",
      "Restricts HTTP/3 connections to QUIC v2 instead of default QUIC v1. This may help on networks that block QUIC v1 but do not recognize QUIC v2. It does not force QUIC on unsupported sites. Connections may fall back to TCP when the server does not support QUIC v2. Hydrogen flag.",
      kOsAll, SINGLE_VALUE_TYPE_AND_VALUE(switches::kQuicVersion, "RFCv2")},
+    {hydrogen::kHydrogenSplitClientHelloCommandLine,
+     "Random TLS ClientHello splitting",
+     "Splits outgoing TLS ClientHello messages across two TLS records at a random point. The split targets the visible server name, or the ECH extension when ECH is used. This can bypass some domain-based network filtering, but it does not hide DNS queries or destination IP addresses and makes TLS traffic unusual and potentially recognizable. Some middleboxes and TLS endpoints mishandle this behavior and may become inaccessible. Hydrogen flag.",
+     kOsAll, FEATURE_VALUE_TYPE(net::features::kHydrogenSplitClientHello)},
 #endif  /* CHROME_BROWSER_HYDROGEN_FLAG_ENTRIES_H_ */
--- a/net/base/features.cc
+++ b/net/base/features.cc
@@ -29,6 +29,8 @@
 
 namespace net::features {
 
+BASE_FEATURE(kHydrogenSplitClientHello, base::FEATURE_DISABLED_BY_DEFAULT);
+
 BASE_FEATURE(kSetIpv6ProbeFalse, "SetIpv6ProbeFalse", base::FEATURE_DISABLED_BY_DEFAULT);
 
 BASE_FEATURE(kAlpsForHttp2, base::FEATURE_ENABLED_BY_DEFAULT);
--- a/net/base/features.h
+++ b/net/base/features.h
@@ -22,6 +22,9 @@
 
 namespace net::features {
 
+// Splits outgoing TLS ClientHello messages across TLS records.
+NET_EXPORT BASE_DECLARE_FEATURE(kHydrogenSplitClientHello);
+
 NET_EXPORT BASE_DECLARE_FEATURE(kSetIpv6ProbeFalse);
 
 // Enables ALPS extension of TLS 1.3 for HTTP/2, see
