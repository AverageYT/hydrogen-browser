--- a/components/hydrogen_services/hydrogen_services_helpers.cc
+++ b/components/hydrogen_services/hydrogen_services_helpers.cc
@@ -101,6 +101,11 @@ bool ShouldAccessUpdateService(const Pre
          prefs.GetBoolean(prefs::kHydrogenUpdateFetchingEnabled);
 }
 
+bool ShouldAccessUBlockAssets(const PrefService& prefs) {
+    return ShouldAccessServices(prefs) &&
+            prefs.GetBoolean(prefs::kHydrogenUBlockAssetsEnabled);
+}
+
 GURL GetExtensionUpdateURL(const PrefService& prefs) {
     if (!ShouldAccessExtensionService(prefs)) {
         return GetDummyURL();
@@ -144,6 +149,14 @@ GURL GetBrowserUpdateURL(const PrefServi
 #endif
 }
 
+GURL GetUBlockAssetsURL(const PrefService& prefs) {
+    if (!ShouldAccessUBlockAssets(prefs)) {
+        return GetDummyURL();
+    }
+
+    return GetServicesBaseURL(prefs).Resolve("/ubo/assets.json");
+}
+
 void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
     PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer) {
   registrar.Add(prefs::kHydrogenServicesEnabled, observer);
--- a/components/hydrogen_services/hydrogen_services_helpers.h
+++ b/components/hydrogen_services/hydrogen_services_helpers.h
@@ -23,12 +23,14 @@ bool ShouldAccessServices(const PrefServ
 COMPONENT_EXPORT(HYDROGEN) bool ShouldFetchBangs(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) bool ShouldAccessExtensionService(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) bool ShouldAccessUpdateService(const PrefService& prefs);
+COMPONENT_EXPORT(HYDROGEN) bool ShouldAccessUBlockAssets(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) GURL GetServicesBaseURL(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) GURL GetDummyURL();
 COMPONENT_EXPORT(HYDROGEN) GURL GetExtensionUpdateURL(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) GURL GetWebstoreSnippetURL(const PrefService& prefs, std::string_view id);
 COMPONENT_EXPORT(HYDROGEN) GURL GetSpellcheckURL(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) GURL GetBrowserUpdateURL(const PrefService& prefs);
+COMPONENT_EXPORT(HYDROGEN) GURL GetUBlockAssetsURL(const PrefService& prefs);
 COMPONENT_EXPORT(HYDROGEN) std::optional<GURL> GetValidUserOverridenURL(std::string_view user_url_);
 COMPONENT_EXPORT(HYDROGEN) void ConfigurePrefChangeRegistrarFor(std::string_view pref_name,
                             PrefChangeRegistrar& registrar, const base::RepeatingClosure& observer);
--- a/components/hydrogen_services/pref_names.h
+++ b/components/hydrogen_services/pref_names.h
@@ -39,6 +39,8 @@ inline constexpr char kHydrogenUpdateFetch
 inline constexpr char kHydrogenSpellcheckEnabled[] =
     "hydrogen.services.spellcheck_files";
 
+inline constexpr char kHydrogenUBlockAssetsEnabled[] =
+    "hydrogen.services.ublock_assets";
 }  // namespace prefs
 
 #endif  // COMPONENTS_HYDROGEN_SERVICES_PREF_NAMES_H_
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -432,6 +432,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHydrogenUpdateFetchingEnabled] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kHydrogenUBlockAssetsEnabled] =
+      settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHydrogenServicesOrigin] =
       settings_api::PrefType::kString;
   (*s_allowlist)[::prefs::kHydrogenServicesConsented] =
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -233,6 +233,7 @@ void RegisterBrowserUserPrefs(user_prefs
     registry->RegisterBooleanPref(prefs::kHydrogenDidOnboarding, false);
     registry->RegisterBooleanPref(prefs::kHydrogenServicesConsented, false);
     registry->RegisterBooleanPref(prefs::kHydrogenUpdateFetchingEnabled, true);
+    registry->RegisterBooleanPref(prefs::kHydrogenUBlockAssetsEnabled, true);
   }
 
   registry->RegisterBooleanPref(
--- a/chrome/browser/extensions/extension_service.cc
+++ b/chrome/browser/extensions/extension_service.cc
@@ -31,6 +31,7 @@
 #include "base/strings/utf_string_conversions.h"
 #include "base/syslog_logging.h"
 #include "base/task/single_thread_task_runner.h"
+#include "base/task/thread_pool.h"
 #include "base/threading/thread_restrictions.h"
 #include "base/time/time.h"
 #include "base/trace_event/trace_event.h"
@@ -68,6 +69,9 @@
 #include "chrome/common/pref_names.h"
 #include "chrome/common/url_constants.h"
 #include "components/crx_file/id_util.h"
+#include "components/hydrogen_services/extension_ids.h"
+#include "components/hydrogen_services/hydrogen_services_helpers.h"
+#include "components/hydrogen_services/pref_names.h"
 #include "components/policy/core/common/policy_pref_names.h"
 #include "components/supervised_user/core/browser/supervised_user_preferences.h"
 #include "content/public/browser/browser_thread.h"
@@ -209,6 +213,7 @@ ExtensionService::ExtensionService(
       ready_(ready),
       updater_(ExtensionUpdater::Get(profile)),
       component_loader_(ComponentLoader::Get(profile_)),
+      can_access_ublock_assets_(hydrogen::ShouldAccessUBlockAssets(*profile_->GetPrefs())),
       error_controller_(error_controller),
       external_install_manager_(ExternalInstallManager::Get(profile)),
       extension_registrar_delegate_(
@@ -290,6 +295,33 @@ ExtensionService::ExtensionService(
       prefs::kExtensionsUIDeveloperMode,
       base::BindRepeating(&ExtensionService::OnDeveloperModePrefChanged,
                           base::Unretained(this)));
+  hydrogen::ConfigurePrefChangeRegistrarFor(
+    prefs::kHydrogenUBlockAssetsEnabled,
+    pref_change_registrar_,
+    base::BindRepeating(&ExtensionService::ToggleUBlockExtension,
+                        base::Unretained(this)));
+}
+
+void ExtensionService::ToggleUBlockExtension() {
+  if (!extension_registrar_) {
+    return;
+  }
+
+  bool has_permission_now = hydrogen::ShouldAccessUBlockAssets(*profile_->GetPrefs());
+  if (can_access_ublock_assets_ == has_permission_now) {
+    return;
+  }
+
+  can_access_ublock_assets_ = has_permission_now;
+
+  for (int seconds : {0, 5}) {
+    content::GetUIThreadTaskRunner({base::TaskPriority::BEST_EFFORT})->PostDelayedTask(
+        FROM_HERE,
+        base::BindOnce(&ExtensionRegistrar::ReloadExtension,
+                        extension_registrar_->GetWeakPtr(),
+                        hydrogen::kUBlockOriginComponentId),
+        base::Seconds(seconds));
+  }
 }
 
 base::WeakPtr<ExtensionServiceInterface> ExtensionService::AsWeakPtr() {
--- a/chrome/browser/extensions/extension_service.h
+++ b/chrome/browser/extensions/extension_service.h
@@ -169,6 +169,8 @@ class ExtensionService : public Extensio
   // KeyedService two-phase shutdown.
   void Shutdown();
 
+  void ToggleUBlockExtension();
+
   // Performs action based on verdicts received from the Extension Telemetry
   // server. Currently, these verdicts are limited to off-store extensions.
   void PerformActionBasedOnExtensionTelemetryServiceVerdicts(
@@ -359,6 +361,8 @@ class ExtensionService : public Extensio
   // Used in test to ensure the service's shutdown method has been called.
   bool is_shut_down_executed_ = false;
 
+  bool can_access_ublock_assets_;
+
   // The controller for the UI that alerts the user about any blocklisted
   // extensions. Not owned.
   raw_ptr<ExtensionErrorController> error_controller_ = nullptr;
--- a/extensions/browser/api/storage/BUILD.gn
+++ b/extensions/browser/api/storage/BUILD.gn
@@ -60,6 +60,7 @@ source_set("storage") {
     ":settings_namespace",
     ":settings_observer",
     "//base",
+    "//components/hydrogen_services",
     "//components/keyed_service/content",
     "//components/value_store",
     "//content/public/browser",
--- a/extensions/browser/api/storage/storage_frontend.cc
+++ b/extensions/browser/api/storage/storage_frontend.cc
@@ -20,6 +20,8 @@
 #include "base/strings/string_util.h"
 #include "base/strings/stringprintf.h"
 #include "base/trace_event/trace_event.h"
+#include "chrome/browser/profiles/profile.h"
+#include "components/hydrogen_services/hydrogen_services_helpers.h"
 #include "components/value_store/value_store_factory.h"
 #include "content/public/browser/browser_context.h"
 #include "content/public/browser/browser_thread.h"
@@ -276,6 +278,24 @@ void StorageFrontend::OnReadFinished(
 
   if (success) {
     get_result.data = result.PassSettings();
+
+    Profile* profile_ = Profile::FromBrowserContext(browser_context_);
+
+    if (Manifest::IsUBlockComponent(extension_id)
+        && storage_area == StorageAreaNamespace::kManaged
+        && hydrogen::ShouldAccessUBlockAssets(*profile_->GetPrefs())) {
+
+      if (!get_result.data->FindDict("adminSettings")) {
+        get_result.data->Set("adminSettings", base::DictValue());
+      }
+
+      base::DictValue* adminSettings = get_result.data->FindDict("adminSettings");
+
+      if (!adminSettings->Find("assetsBootstrapLocation")) {
+        adminSettings->Set("assetsBootstrapLocation",
+          base::Value(hydrogen::GetUBlockAssetsURL(*profile_->GetPrefs()).spec()));
+      }
+    }
   }
 
   std::move(callback).Run(std::move(get_result));
--- a/third_party/ublock/js/assets.js
+++ b/third_party/ublock/js/assets.js
@@ -525,7 +525,7 @@ let assetSourceRegistry = Object.create(
 function getAssetSourceRegistry() {
     if ( assetSourceRegistryPromise === undefined ) {
         assetSourceRegistryPromise = cacheStorage.get(
-            'assetSourceRegistry'
+            'assetSourceRegistry' + (µb.assetsBootstrapLocation ?? '')
         ).then(bin => {
             if ( bin instanceof Object ) {
                 if ( bin.assetSourceRegistry instanceof Object ) {
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -1776,6 +1776,12 @@
         Hydrogen will automatically check for component updates and install them. We recommend keeping this setting enabled. On Linux, browser updates are handled by your distro's package manager. Please check for Hydrogen package updates at least every week.
       </message>
     </if>
+    <message name="IDS_SETTINGS_HYDROGEN_SERVICES_UBO" desc="Description of the toggle for uBO filter lists">
+      Allow downloading filter lists for uBlock Origin
+    </message>
+    <message name="IDS_SETTINGS_HYDROGEN_SERVICES_UBO_DESCRIPTION" desc="Description of the toggle for automatic update downloads">
+      Hydrogen will fetch fresh filter lists for uBlock Origin. All requests to lists are proxied to protect your privacy. When disabled, default filter lists will be loaded from local storage, which are only updated along with Hydrogen. Optional filter lists will be requested without proxying.
+    </message>
     <message name="IDS_SETTINGS_HYDROGEN_SERVICES_OVERRIDE" desc="Text input for overriding the Hydrogen services server">
       Use your own instance of Hydrogen services
     </message>
--- a/chrome/browser/resources/settings/privacy_page/services_page.html
+++ b/chrome/browser/resources/settings/privacy_page/services_page.html
@@ -104,6 +104,11 @@
                 label="$i18n{hydrogenUpdatesToggle}"
                 sub-label="$i18n{hydrogenUpdatesToggleDescription}">
             </settings-toggle-button>
+            <settings-toggle-button id="filterlistToggleButton"
+                pref="{{prefs.hydrogen.services.ublock_assets}}"
+                label="$i18n{hydrogenUboToggle}"
+                sub-label="$i18n{hydrogenUboToggleDescription}">
+            </settings-toggle-button>
         </cr-collapse>
     </div>
     </template>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -2398,6 +2398,10 @@ void AddPrivacyStrings(content::WebUIDat
       {"hydrogenUpdatesToggle", IDS_SETTINGS_HYDROGEN_SERVICES_UPDATE},
       {"hydrogenUpdatesToggleDescription",
         IDS_SETTINGS_HYDROGEN_SERVICES_UPDATE_DESCRIPTION},
+      {"hydrogenUboToggle",
+        IDS_SETTINGS_HYDROGEN_SERVICES_UBO},
+      {"hydrogenUboToggleDescription",
+        IDS_SETTINGS_HYDROGEN_SERVICES_UBO_DESCRIPTION},
       {"hydrogenOriginOverride", IDS_SETTINGS_HYDROGEN_SERVICES_OVERRIDE},
       {"hydrogenOriginOverrideDescription",
         IDS_SETTINGS_HYDROGEN_SERVICES_OVERRIDE_DESCRIPTION},
