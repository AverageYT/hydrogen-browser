--- a/chrome/browser/hydrogen_flag_choices.h
+++ b/chrome/browser/hydrogen_flag_choices.h
@@ -39,6 +39,8 @@ namespace hydrogen {
   constexpr const char kHydrogenSplitClientHelloCommandLine[] =
       "hydrogen-split-clienthello";
 
+  constexpr const char kMiddleClickAutoscrollCommandLine[] = "middle-click-autoscroll";
+
 }  // namespace hydrogen
 
 #endif  /* CHROME_BROWSER_HYDROGEN_FLAG_CHOICES_H_ */
--- a/chrome/browser/hydrogen_flag_entries.h
+++ b/chrome/browser/hydrogen_flag_entries.h
@@ -43,4 +43,8 @@
      "Random TLS ClientHello splitting",
      "Splits outgoing TLS ClientHello messages across two TLS records at a random point. The split targets the visible server name, or the ECH extension when ECH is used. This can bypass some domain-based network filtering, but it does not hide DNS queries or destination IP addresses and makes TLS traffic unusual and potentially recognizable. Some middleboxes and TLS endpoints mishandle this behavior and may become inaccessible. Hydrogen flag.",
      kOsAll, FEATURE_VALUE_TYPE(net::features::kHydrogenSplitClientHello)},
+    {hydrogen::kMiddleClickAutoscrollCommandLine,
+     "Middle Click Autoscroll",
+     "Enables autoscroll on middle click. Hydrogen flag, Chromium feature.",
+     kOsDesktop, FEATURE_VALUE_TYPE(blink::features::kHydrogenMiddleClickAutoscroll)},
 #endif  /* CHROME_BROWSER_HYDROGEN_FLAG_ENTRIES_H_ */
--- a/third_party/blink/public/common/features.h
+++ b/third_party/blink/public/common/features.h
@@ -45,6 +45,8 @@ BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenNoiseAudio);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenNoiseCpuCores);
 
+BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenMiddleClickAutoscroll);
+
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kAIPageContentIncludePopupWindows);
--- a/third_party/blink/common/features.cc
+++ b/third_party/blink/common/features.cc
@@ -19,6 +19,15 @@
 
 namespace blink::features {
 
+BASE_FEATURE(kHydrogenMiddleClickAutoscroll,
+             "HydrogenMiddleClickAutoscroll",
+#if BUILDFLAG(IS_WIN)
+             base::FEATURE_ENABLED_BY_DEFAULT
+#else
+             base::FEATURE_DISABLED_BY_DEFAULT
+#endif
+);
+
 BASE_FEATURE(kDisableLinkDrag, "DisableLinkDrag", base::FEATURE_DISABLED_BY_DEFAULT);
 BASE_FEATURE(kReducedSystemInfo, "ReducedSystemInfo", base::FEATURE_DISABLED_BY_DEFAULT);
 BASE_FEATURE(kRemoveClientHints, "RemoveClientHints", base::FEATURE_DISABLED_BY_DEFAULT);
--- a/third_party/blink/renderer/core/exported/web_view_impl.cc
+++ b/third_party/blink/renderer/core/exported/web_view_impl.cc
@@ -1911,9 +1911,9 @@ void WebView::ApplyWebPreferences(const
       prefs.default_maximum_page_scale_factor);
 #endif
 
-#if BUILDFLAG(IS_WIN)
-  RuntimeEnabledFeatures::SetMiddleClickAutoscrollEnabled(true);
-#endif
+  RuntimeEnabledFeatures::SetMiddleClickAutoscrollEnabled(
+    base::FeatureList::IsEnabled(features::kHydrogenMiddleClickAutoscroll)
+  );
 
   RuntimeEnabledFeatures::SetTranslateServiceEnabled(
       prefs.translate_service_available);
