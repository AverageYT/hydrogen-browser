--- a/chrome/browser/hydrogen_flag_choices.h
+++ b/chrome/browser/hydrogen_flag_choices.h
@@ -32,6 +32,8 @@ namespace hydrogen {
   constexpr const char kHydrogenNoiseCommandLine[] = "hydrogen-noise";
   constexpr const char kHydrogenNoiseCanvasCommandLine[] = "hydrogen-noise-canvas";
   constexpr const char kHydrogenNoiseAudioCommandLine[] = "hydrogen-noise-audio";
+  constexpr const char kHydrogenNoiseCpuCoresCommandLine[] =
+      "hydrogen-noise-hw-concurrency";
 
 }  // namespace hydrogen
 
--- a/chrome/browser/hydrogen_flag_entries.h
+++ b/chrome/browser/hydrogen_flag_entries.h
@@ -26,4 +26,9 @@
      "[Hydrogen Noise] Jitter audio context data",
      "Adds insignificant jitter to audio data to deceive AudioContext-based fingerprinting. Hydrogen flag.",
      kOsAll, FEATURE_VALUE_TYPE(blink::features::kHydrogenNoiseAudio)},
+    {hydrogen::kHydrogenNoiseCpuCoresCommandLine,
+     "[Hydrogen Noise] Randomize number of CPU cores",
+     "Randomizes the number of cores returned by "
+     "`navigator.hardwareConcurrency` in a reasonable range. Hydrogen flag.",
+     kOsAll, FEATURE_VALUE_TYPE(blink::features::kHydrogenNoiseCpuCores)},
 #endif  /* CHROME_BROWSER_HYDROGEN_FLAG_ENTRIES_H_ */
--- a/content/browser/hydrogen_noise/noise_token_data.cc
+++ b/content/browser/hydrogen_noise/noise_token_data.cc
@@ -59,6 +59,8 @@ HydrogenNoiseTokenData::HydrogenNoiseTokenDa
       blink::HydrogenNoiseToken(base::RandUint64());
   session_tokens_[blink::HydrogenNoiseFeature::kAudio] =
       blink::HydrogenNoiseToken(base::RandUint64());
+  session_tokens_[blink::HydrogenNoiseFeature::kHardware] =
+      blink::HydrogenNoiseToken(base::RandUint64());
 }
 
 // static
--- a/third_party/blink/common/features.cc
+++ b/third_party/blink/common/features.cc
@@ -51,6 +51,9 @@ BASE_FEATURE(kHydrogenNoiseCanvas,
 BASE_FEATURE(kHydrogenNoiseAudio,
              base::FEATURE_ENABLED_BY_DEFAULT);
 
+// navigator.hardwareConcurrency noising for fingerprint protection.
+BASE_FEATURE(kHydrogenNoiseCpuCores, base::FEATURE_ENABLED_BY_DEFAULT);
+
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
 BASE_FEATURE(kAIPageContentIncludePopupWindows,
--- a/third_party/blink/public/common/features.h
+++ b/third_party/blink/public/common/features.h
@@ -43,6 +43,7 @@ BLINK_COMMON_EXPORT extern const base::F
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenNoise);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenNoiseCanvas);
 BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenNoiseAudio);
+BLINK_COMMON_EXPORT BASE_DECLARE_FEATURE(kHydrogenNoiseCpuCores);
 
 // Controls whether to include information about the page's open popup in
 // AIPageContent.
--- a/third_party/blink/public/common/hydrogen_noise/noise_token.h
+++ b/third_party/blink/public/common/hydrogen_noise/noise_token.h
@@ -23,6 +23,7 @@ namespace blink {
 enum class HydrogenNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // A wrapper class for a uint64_t noise token.
--- a/third_party/blink/public/mojom/hydrogen_noise/noise_token.mojom
+++ b/third_party/blink/public/mojom/hydrogen_noise/noise_token.mojom
@@ -8,6 +8,7 @@ module blink.mojom;
 enum HydrogenNoiseFeature {
   kCanvas,
   kAudio,
+  kHardware,
 };
 
 // Typemapped to blink::HydrogenNoiseToken
--- a/third_party/blink/renderer/core/execution_context/navigator_base.cc
+++ b/third_party/blink/renderer/core/execution_context/navigator_base.cc
@@ -9,6 +9,7 @@
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/frame/navigator_concurrent_hardware.h"
+#include "third_party/blink/renderer/core/hydrogen_noise/hydrogen_noise_hash.h"
 #include "third_party/blink/renderer/core/probe/core_probes.h"
 #include "third_party/blink/renderer/platform/runtime_enabled_features.h"
 #include "third_party/blink/renderer/platform/wtf/text/string_builder.h"
@@ -41,6 +42,52 @@ String GetReducedNavigatorPlatform() {
 #endif
 }
 
+constexpr const unsigned kMinNoisedCores = 2;
+constexpr const unsigned kMaxNoisedCores = 16;
+
+std::vector<unsigned> PossibleValuesForCoreCount(unsigned n) {
+  // Clamp the number of cores between [2..=16], as other
+  // configurations are less common.
+  n = std::clamp(n, kMinNoisedCores, kMaxNoisedCores);
+
+  // A machine with a odd number of CPUs is very uncommon.
+  // Floor it to the lower even number.
+  if (n % 2) {
+    n -= 1;
+  }
+
+  // If we can subtract some amount and still end up with
+  // a common CPU count (which is the case for most configurations)
+  // let's add it to the list of possible values.
+  std::vector<unsigned> candidates = {n};
+  if (n > 4) {
+    candidates.push_back(n - 4);
+  }
+
+  if (n > 2) {
+    candidates.push_back(n - 2);
+  }
+
+  return candidates;
+}
+
+unsigned MaybeNoiseCoreCount(ExecutionContext* execution_context, unsigned n) {
+  if (!base::FeatureList::IsEnabled(blink::features::kHydrogenNoiseCpuCores) ||
+      !execution_context ||
+      !execution_context->GetHydrogenNoiseTokens().contains(
+          HydrogenNoiseFeature::kHardware)) {
+    return n;
+  }
+
+  auto token_hash =
+      HydrogenNoiseHash(execution_context->GetHydrogenNoiseTokens().at(
+          HydrogenNoiseFeature::kHardware));
+  token_hash.Update(n);
+
+  auto values = PossibleValuesForCoreCount(n);
+  return values[token_hash.GetValueBelow(values.size())];
+}
+
 }  // namespace
 
 NavigatorBase::NavigatorBase(ExecutionContext* context)
@@ -77,7 +124,7 @@ unsigned int NavigatorBase::hardwareConc
 
   probe::ApplyHardwareConcurrencyOverride(
       probe::ToCoreProbeSink(GetExecutionContext()), hardware_concurrency);
-  return hardware_concurrency;
+  return MaybeNoiseCoreCount(GetExecutionContext(), hardware_concurrency);
 }
 
 ExecutionContext* NavigatorBase::GetUAExecutionContext() const {
--- a/third_party/blink/public/common/hydrogen_noise/noise_token_mojom_traits.h
+++ b/third_party/blink/public/common/hydrogen_noise/noise_token_mojom_traits.h
@@ -30,6 +30,8 @@ struct BLINK_COMMON_EXPORT
         return blink::mojom::HydrogenNoiseFeature::kCanvas;
       case blink::HydrogenNoiseFeature::kAudio:
         return blink::mojom::HydrogenNoiseFeature::kAudio;
+      case blink::HydrogenNoiseFeature::kHardware:
+        return blink::mojom::HydrogenNoiseFeature::kHardware;
     }
     NOTREACHED();
   }
@@ -41,6 +43,8 @@ struct BLINK_COMMON_EXPORT
         return blink::HydrogenNoiseFeature::kCanvas;
       case blink::mojom::HydrogenNoiseFeature::kAudio:
         return blink::HydrogenNoiseFeature::kAudio;
+      case blink::mojom::HydrogenNoiseFeature::kHardware:
+        return blink::HydrogenNoiseFeature::kHardware;
     }
 
     NOTREACHED();
